import React from 'react';

/**
 * If a url is hit that a route doesn't exist for
 * @returns {*}
 * @constructor
 */
const NotFound = () => {
  return(
    <div className="four-oh-four">
      404 Error!
    </div>
  )
};

export default NotFound;